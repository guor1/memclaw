# 05 — 下一阶段计划

> 制定日期：2026-08-29。权威设计源：[04-详细设计文档.md](../design/04-详细设计文档.md)。
> 本文把「设计承诺 vs 已落地」的缺口 + 近期实战暴露的问题，收敛成一份可执行的阶段计划。
> 每完成一项，勾掉并在末尾变更记录追加一行。

## 0. 现状与指导原则

M1–M6 主体已落地：对话 / 持久化 / 记忆检索(Lane1) / 防卡死 / 工具+审批 / cron 主动性 / CLI。
但近期（2026-08-29 前后）实战暴露：**咬人的问题全在核心对话闭环里**——工具调用空回复、
输出截断、abort 失灵。设计文档的缺口清单只跟踪「功能没接线」，没跟踪「核心路径不稳」。

**指导原则**：在会丢事件、abort 不生效的对话路径上堆功能，等于流沙盖楼——每个新功能都继承
这份 flaky，且更难 debug。因此本计划排序为：

```
P0 稳定核心闭环  →  P1 补齐助手功能  →  P2 生产化收口
（发消息/工具/中止可靠）  （像个完整助手）    （能长期挂着、可发布）
```

已具备的排查抓手：本轮已落地结构化日志（`~/.oc/logs/`）+ `oc debug` 运行时快照，
P0 的问题可先用日志复现验证根因，再动手，避免猜。

---

## P0 — 稳定核心闭环（最高优先）

来源：2026-08-29 线程安全排查（三 crate 分头审）。结论是**无经典死锁/数据竞争**，
DashMap 用法规范、actor 隔离良好；真正的风险是**事件丢失**和**审批路径**。

### P0-1 事件丢失（极可能是「截断」根因）🔴 ✅ 已完成（2026-08-31）
> **落地**：采用「per-run 背压流」（方案 B，见 [06-zeroclaw流式传输调研.md](../research/zeroclaw流式传输调研.md)）。
> 新增 `RunSink`（[sink.rs](../../crates/oc-server/src/sink.rs)）：run 内联事件（Assistant/
> Lifecycle/Tool/Approval）改走每 run 专属、有界、背压的通道，经 `RunSink::Conn` 定向
> 回发到发起连接的出站队列；慢客户端 → 背压挂起（driver select cancel，不锁车道）→
> 不丢字。broadcast 瘦身到仅 Usage/Proactive（低频、不会 Lagged）。
> 回归测试 `long_reply_not_truncated`：400 delta 经真实连接完整送达。
- **现象**：长回复中途截断、消息不完整——本周多次遇到。
- **根因（两处叠加）**：
  - broadcast 容量固定 256（[lib.rs](../../crates/oc-server/src/lib.rs) `EVENT_CHANNEL_CAP`）。流式回复每 token
    一个 `Assistant` delta，长回复轻松超 256；任一慢连接拖慢转发即 `Lagged`，丢失的事件**永久丢失**。
  - 每连接转发任务 `try_send` 失败**静默丢帧**（[conn.rs](../../crates/oc-server/src/conn.rs)），不断连、不告警。
- **修法**：
  1. 先用日志复现确认：长回复时 `conn.rs` 是否真的 `Lagged`/丢帧（加计数日志）。
  2. delta 事件合并/批处理，降低事件频率（一次 flush 多个 token）。
  3. `Lagged` 后触发一次状态重同步（重新拉当前 run 完整文本）而非放任缺口；
     慢连接宁可断连也不静默丢。
- **验证**：构造 >256 token 的长回复，确认 TUI 完整收到。

### P0-2 CancellationToken 打断不了正在等待的审批 🟠 ✅ 已完成（2026-08-31）
> **落地**：工具侧 [exec.rs](../../crates/oc-tools/src/exec.rs) 的 `gate.ask().await` 套
> `select!` + `cancel.cancelled()` → 返回 `Aborted`；server 侧审批等待
> （[tools_bridge.rs](../../crates/oc-server/src/tools_bridge.rs) `request_approval`）同样
> `select!` 叠 cancel。回归测试 `abort_interrupts_pending_approval_and_cleans_registry`
> 验证 abort 立即释放车道（<2s）。
- **现象**：用户发 abort（或看门狗判卡死）时，run 卡在 `gate.ask().await` 不释放车道，abort 看似失灵。
- **根因**：审批等待没有 select 在 cancel 上（[run.rs](../../crates/oc-server/src/run.rs) 的 `exec_tool().await` 未套 cancel；
  [exec.rs](../../crates/oc-tools/src/exec.rs) 的 `gate.ask().await` 无取消守卫）。
- **修法**：审批等待 `tokio::select!` 叠加 `cancel.cancelled()` → 返回 `Aborted`；
  `ApprovalGate::ask` 观察 cancel token。

### P0-3 审批 registry entry 泄漏 🟠 ✅ 已完成（2026-08-31）
> **落地**：[tools_bridge.rs](../../crates/oc-server/src/tools_bridge.rs) 引入 `ApprovalGuard`
> RAII 守卫，按 approval_id 在 drop 时清理 registry，覆盖所有退出路径（正常回执/超时/
> abort/pump 被 abort）。同回归测试断言 abort 后 `registry.is_empty()`。
> 顺带简化：删除 `ApprovalHandler` trait 与 `EventApprovalHandler`（审批逻辑内聚到
> `ToolExecutor::run`，registry 与 ServerState 共享）。
- **根因**：超时/abort 导致 `rx.await` 被 drop 时，`ApprovalRegistry` 里的 entry 不清理
  （[tools_bridge.rs](../../crates/oc-server/src/tools_bridge.rs) 只在 channel-closed 分支 remove）。长期运行无界增长。
- **修法**：用 RAII guard（按 approval_id）或每个退出路径都 remove。

### P0-4 阻塞 IO 跑在 async executor 上 🟠 ✅ 已完成（2026-08-31）
> **落地**：[file.rs](../../crates/oc-tools/src/file.rs) 的 `grep`/`glob` 包
> `tokio::task::spawn_blocking`，同步遍历不再霸占 tokio worker，工具级 30s 超时也对其生效。
- **根因**：`grep`/`glob`（[file.rs](../../crates/oc-tools/src/file.rs)）用同步 `walkdir`/`std::fs`/`canonicalize`，
  直接在 async `invoke` 里跑；大目录会卡住整个 tokio worker，拖累所有会话。
- **修法**：包 `tokio::task::spawn_blocking`。工具 30s 超时对同步阻塞代码也不生效，一并解决。

### P0 出口标准
发消息、工具调用、中止 三条基础路径可靠：长回复不截断、abort 立即释放车道、
大目录工具不冻结其它会话、长期运行审批表不泄漏。这是后续一切的地基。

---

## P1 — 补齐助手功能

设计承诺但未落地的实质功能。大部分**纯函数已在 oc-core 就绪**，是「接线」工作。
排序按「用户可感知价值 / 接线成本」。

### P1-1 ask_user 工具（需先做，与 P0-2/P0-3 的审批通道重叠）✅ 已完成（2026-08-31）
> **落地**：新增自由文本回执通道，与审批通道完全同构（复用 P0-2 取消 + P0-3 RAII 清理）。
> - proto：加 `InputId`、`Event::UserInput`（prompt）、`Method::UserReply`（自由文本，`None`=取消）。
> - tools：`InputGate`/`InputRequest`（[types.rs](../../crates/oc-tools/src/types.rs)）+ 新工具
>   [ask_user.rs](../../crates/oc-tools/src/ask_user.rs)（阻塞等答，`select!` 叠 cancel）。
> - server：`InputRegistry`（[state.rs](../../crates/oc-server/src/state.rs)）+ `request_input` + `InputGuard`
>   RAII（[tools_bridge.rs](../../crates/oc-server/src/tools_bridge.rs)，与 approval 分支并列）+
>   `user.reply` 分派（[dispatch.rs](../../crates/oc-server/src/dispatch.rs)）+ lib 接线。
> - TUI：`pending_input` 输入态——收 `UserInput` 进自由文本模式，Enter 送答/Esc 取消
>   （[app.rs](../../crates/oc-tui/src/app.rs)）。
> - 回归：`ask_user`（全链路回答回喂 + registry 不泄漏；abort 打断等待 + registry 清空）。
- **现状**：未实现，缺自由文本回传通道（当前只有 y/n 审批）。
- **设计**（[04 §6.1](../design/04-详细设计文档.md)）：主动提问，阻塞 run 等答复。
- **改动**：proto 加 `UserInput` 事件/方法 + ApprovalRegistry 式文本回执 + TUI 输入态。
- **为何紧接 P0**：文本回执通道的取消/泄漏处理与 P0-2/P0-3 同源，一起做省一次重构。

### P1-2 standing intent 触发链接线 ✅ 已完成（2026-08-31）
> **落地**：详见 [07-P1-2-standing-intent方案.md](P1-2-standing-intent方案.md)（含 zeroclaw 调研）。
> 触发链在 [session.rs](../../crates/oc-server/src/session.rs) `intent_scan`：`begin_run` 里
> `load_history` 后 → `intent_list` → `core::intent_prefilter` 词法命中 → 每条用**自己行上**的
> cooldown/budget/expiry 走 `allow_fire` → 允许则复用 bootstrap 注入管线注入「待办提醒」+
> `intent_mark_fired`+审计；拒绝静默跳过。store 补 `intent_add/list/rm/mark_fired`
> （[ops.rs](../../crates/oc-store/src/ops.rs)）；创建入口照 cron 抄 `intent.add/list/rm` proto+
> `oc intent` CLI；anti-nagging 默认从 `[proactive]` 配置灌入（此前是死键）。
> 修 bug：绝对 expiry_at→相对 expiry_secs 换算不能用 `.max(0)`（0=永不过期，会把已过期翻转）。
> 新增回归：store `intent_crud_roundtrip`、server `standing_intent`（命中注入/无关不注入/
> cooldown 抑制/budget 用尽/过期不触发/每轮上限）、core 老配置缺键兼容。全工作区测试通过。
- **现状**：纯函数 `intent_prefilter`/`allow_fire` 就绪，`standing_intent` 表无 ops，未接入 session。
- **改动**：补 store ops + session Submit 钩子（入站消息 → 预筛 → allow_fire → 注入隐藏上下文）。
- **成本**：小，接线为主。
- **调研结论**：zeroclaw **无此能力**（拆成向量记忆召回/cron/SOP 三块，无一是「入站自由文本命中
  话题→注入待办提醒」）。借鉴其「anti-nagging 挂触发实体上」「创建用显式而非自动解析」。
- **未做（留 P1-5）**：把 intent 暴露为模型可直接调用的工具（对标 cron 工具化）。

### P1-3 偏好 supersede 接线 ✅ 已完成（2026-09-01）
> **落地**：`persist_explicit_memory`（[session.rs](../../crates/oc-server/src/session.rs)）接
> `extract_pref_key`（新增纯函数）→ `memory_by_pref_key` 查同主题 → `supersede` 判
> Replace/Add/Ignore；Replace 时先写新后删旧 + 审计。`memory` 表加 `pref_key` 列（迁移 v2，
> `ALTER TABLE ADD COLUMN` 平滑升级老库）。**落点是 DB 而非 USER.md 文件**——探查发现
> USER.md 是 onboard 建了但无人读写的死文件，偏好实际都在 `memory` 表走 Lane1 注入。
> 效果："记住我用 VS Code" 后说"记住我改用 Neovim 了"，旧条目被顶掉而非并列矛盾两条。
> 修 bug：给 `search_candidates` 的 row mapper 加了 `pref_key` 取值却漏改它动态拼的 SELECT
> 列表（另两处是静态 SQL 已改），被既有 Lane1 注入测试当场抓到。
- **现状**：纯函数 `supersede` 无调用方。
- **改动**：在偏好写入路径调用，让新事实就地替换矛盾项而非 append。
- **修正认知**：原以为现状是"纯 append"，实际已按 `id=mem-{content_hash}` 做**逐字去重**；
  真正的缺口是**同主题不同表述**（"我用 VS Code" vs "我改用 Neovim" 哈希不同 → 并存矛盾）。
- **未做**：USER.md 文件读写（→ P1-4，与 dreaming 重写 MEMORY.md 同属文件落盘+乐观并发）；
  模型抽 key（确定性优先，词表不够用再议）；向量语义判同主题（→ P2）。

### P1-4 dreaming 重写 MEMORY.md ✅ 已完成（2026-09-01）
> **落地**：`oc-server::dreaming::scan_with` 在 DB 内 tier 提升之后追加**巩固模型轮**：
> 读 MEMORY.md 算 hash → `build_consolidation_prompt`（既有内容+新条目一起给，令模型
> **合并**而非丢弃历史）→ 调模型 → **落盘前再读一次**算 hash → `core::decide_write` 判定
> → 一致则同目录 `.tmp` + 原子 rename 覆盖；不一致（用户手改了）则**退化追加**、不覆盖。
> 纯策略在 [dreaming.rs](../../crates/oc-core/src/dreaming.rs)（prompt 组装 + `decide_write`），
> 文件 IO 与模型轮在 [dreaming.rs](../../crates/oc-server/src/dreaming.rs)。
> 路径经 `SessionConfig::soul_dir` 由 CLI 传入；`None` 时只做 DB 内巩固、不碰文件
> （既有 `scan()` 签名保留，等价于 `scan_with(.., None)`）。
> 防护：模型空输出/超时 → 保持原文件（不清空长期记忆）；系统提示词明确禁止编造
> （夜间无人监督的重写掺入幻觉会污染每轮注入的 curated 核心）；Windows 上 rename
> 不能覆盖已存在文件，故先删目标；失败清理 `.tmp`。
- **现状**：仅 DB 内 tier 提升（episodic→curated），不写文件、不跑「巩固模型轮」。
- **改动**：加巩固模型轮（读候选 → 模型重写 → 乐观并发写 MEMORY.md，见 [04 §11.4](../design/04-详细设计文档.md)）。
- **未做**：巩固轮的**每日 token 预算上限**（[04 §16 开放问题4]）——当前靠
  `DreamCfg::max_consolidations`（默认 8 条）+ `DREAM_EVERY_TICKS` 稀疏触发间接限流，
  没有显式日预算。真机观察成本后再定，见下方 P2 待办。
- **未做**：USER.md 落盘（需把偏好从 DB 渲染成文件，与「模型重写」不同机制，另开一项）。

### P1-5 定时/延时提醒引导至 cron ✅ 已完成（2026-09-01，含真机复测后的三项补修）
> **落地**：`cron` 工具（[cron.rs](../../crates/oc-tools/src/cron.rs)）带四个 op——
> `add`（重复）/ `delay`（一次性）/ `list` / `rm`，经 `ToolCtx::cron` 门 →
> `handle_cron_op`（[proactive.rs](../../crates/oc-server/src/proactive.rs)）落地。
> 系统提示词（[prompt.rs](../../crates/oc-core/src/prompt.rs)）点明「登记后系统主动推送、
> 不占用当前对话、无需等待」，并禁止 shell 睡眠与轮询查时间。
>
> **首版只做了「暴露 cron_add + 提示引导」，真机复测暴露它并不够用**，补了三项
> （详见下方「真机复测」）：时区落地、`list`/`rm`、亚分钟延时。
- **现象**：用户说「12:50 提醒我喝水」，模型**不用已有的 cron 能力**，而是想在一次 run 内
  **同步阻塞等到 12:50**——反复调 `sys:now` 查时间 + `powershell Start-Sleep`/`timeout /T`
  硬凑「等待」，6 轮工具调用约 76 秒后触发 loop detection、判 `LoopDetected` 中止，提醒没设成。
  （日志：`~/.oc/logs` 中 run `01a05626-0dda`，第 622–653 行。）
- **根因（两层）**：
  1. **能力认知缺失**：cron 定时任务能力已实现（[proactive.rs](../../crates/oc-server/src/proactive.rs) + `cron.add`），
     但模型不知道该用它、也没有可直接调用的 cron 工具，于是选了「run 内干等」这条错路。
  2. **shell 等待不可靠**：PowerShell `Start-Sleep` 在测试机上「只回显不执行」，模型反复换命令试
     → 撞 loop detection。此故障是①的次生，①修好后该路径不会被触发。
- **修法（方向）**：
  - **主**：把 cron 引导进模型决策——系统提示明确「定时/延时/未来某时刻的提醒用 cron，
    不要用 shell 睡眠阻塞等待」；并考虑把 cron.add 暴露为模型可直接调用的工具（现仅 CLI/proto）。
  - **副**：给模型一个可靠的「延时/定时」原语，替代拿 shell 硬凑。
- **归类说明**：run 循环 / 事件流 / 审批 / loop detection 本次**均按设计正确工作**（loop detection
  正确兜住了打转，未无限空转）。这不是 P0 代码回归，是上层「助手能力/引导」缺失，属 P1 范畴。
- **验证**：「X 时提醒我做 Y」应秒设为 cron 定时任务并即时反馈，而非阻塞 run 干等。

#### 真机复测（2026-09-01 当晚）：首版不够用，补三项

首版上线后立刻真机验证，**模型确实改用工具了**（不再拼 shell、不再撞 loop detection），
但提醒**仍然一次都没响**。三个独立缺陷叠在一起：

| # | 缺陷 | 表现 | 根因 |
|---|---|---|---|
| 1 | **时区没落地** 🔴 | 本地 21:46 的提醒被排到**次日 05:46**，差 8 小时 | `next_fire` 一律按 UTC 解释表达式；`tz` 列自 M6 起**存了没人读**（签名里根本没这个参数） |
| 2 | **模型无查询/删除手段** | 用户说「没收到」，模型只能猜，猜成「本环境 cron 送不到」并改用更不可靠的替代方案，反复道歉 | 首版只暴露了 `add`，没给 `list`/`rm` |
| 3 | **亚分钟延时无法表达** | 用户要「90 秒后」，模型只好写成 `46 21 * * *`——白得一条**每天**都响的任务；「10 秒后」直接放弃 | cron 最小粒度是分钟 |

缺陷 1 是**先于 P1-5 就存在**的设计简化（M6 起 tz 就是死列），但 P1-5 把它捅了出来：
以前只有人手敲 CLI，敲的人自己会换算 UTC；模型接手后自然用本地时间写表达式 + 附
`tz=Asia/Shanghai`，以为系统会处理。

补修：
- **时区**：`next_fire(expr, after, tz)` 按 IANA 时区解释（引入 `time-tz`，唯一新依赖）。
  时区名由 CLI 探测本机后经 `SessionConfig::default_tz` 传入——`time` crate 取本地偏移
  在多线程进程里不可靠，daemon 不能自己探。tz 拼错**报错而非静默按 UTC**（静默正是
  这个缺陷难被发现的原因）。DST 跳表/回拨按个人助手场景做了简化，模块头注明。
- **四个 op**：`cron` 工具统一 `add`/`delay`/`list`/`rm`。`list` 回执带**本地墙上时间**
  与剩余时长；`rm` 删不到时明说「没有这条」，免得模型向用户确认「已取消」。
- **一次性延时**：`delay` 存 `@once` 标记 + 绝对 `next_at`，触发后即删；≤1h 的另挂
  精确 tokio timer（心跳是分钟级，短延时靠扫描会迟到近一分钟）。
- **顺带**：`oc cron list` 原先只印 unix 秒，排查时看不出差了 8 小时——改为按行 tz
  渲染本地时间；`oc cron add --tz` 默认值从 `UTC` 改为本机时区。
- **验证**：本地时间准点响；`op=list` 能自查；「10 秒后提醒」秒级准确且只响一次。

### P1-6 episodic 记忆产出（§11.5 flush）✅ 已完成（2026-09-04）
> **落地**：纯策略 `extract_episode_candidates`（[memory.rs](../../crates/oc-core/src/memory.rs)）
> 把 transcript 归成候选——user+assistant **配对**成一条（记住的是「问了 X 答了 Y」这个完整
> 情节，不是半句），无配对的单条按更高字数门槛保留，`tool`/`system` 跳过。寒暄用**长度**滤
> 掉（成对合计 <12 字），不维护寒暄词表——词表永远漏，且会误伤「谢谢，那问题解决了吗」。
> 「记住…」开头的在提取阶段就排除，否则同一事实在 curated/episodic 各存一份、被 dreaming
> 再巩固一次。
>
> 编排在 [session.rs](../../crates/oc-server/src/session.rs) `flush_episodic`，两条触发路径：
> **compact** 在**调摘要模型之前**沉淀（放后面的话摘要失败会连沉淀一起丢，而这批历史正要被
> 有损摘要取代，是它们进长期记忆的最后机会），只沉淀进摘要区的那部分；**session.reset** 走
> `flush_before_reset`，由 dispatch handler 调用（reset 只改 `reset_at` 一列，不经 actor、
> 不占车道）。id 用内容哈希 → 重复 flush 幂等；失败仅告警，不阻塞 compact/reset 本身。
>
> `origin=Agent` 而非 Owner：内容是系统**从对话推断**该记什么，不是用户交代的（§4.2
> 「绝不默认 Owner」）。门2 只排除 Untrusted/System，故 Agent 仍可被巩固，但与用户显式
> 记忆的信任级别分开。
>
> 新增 13 测试（core 提取 7 + server `episodic_flush.rs` 6，后者含**产出物能过 dreaming
> 结构门**的闭环断言）。全工作区 242 用例通过，clippy 零告警。
- **现象**：**dreaming 在真机上永远空转**。P1-4 把巩固模型轮做完了，但整条链的
  上游是断的——全仓**没有任何代码写入 `tier='episodic'`** 的记忆，而
  `dream_candidates` 只取 episodic。所以正常聊天不会积累出任何候选，
  每轮 dreaming 扫描都是空集直接返回。
- **现状核对**（2026-09-01 写 P1-4 测试用例时发现）：
  | 环节 | 状态 |
  |---|---|
  | 产生 episodic 候选 | ✅ P1-6（此前 ❌ 无人写） |
  | `dream_candidates` 取候选 | ✅ |
  | 双门判定 + tier 提升 | ✅ |
  | 巩固模型轮重写 MEMORY.md | ✅ P1-4 |

  此前唯一的记忆写入路径是 `persist_explicit_memory`（用户显式"记住…"），它直接写
  **curated**，绕过了 episodic → 巩固这条路。
- **改动**：落地设计 §11.5——`session.reset` 或压缩（`compact`）触发前，把当前会话中
  「值得记住」的内容 flush 为 episodic 候选。判定用纯函数（哪些算值得记住），
  IO 在 store，调度在 session。
- **为何是 P1**：不做的话 P1-4 的巩固能力**在真实使用中等于不存在**——只能靠手工
  塞数据验证（回归见 [dreaming_memory_md.rs](../../crates/oc-server/tests/dreaming_memory_md.rs)）。
  「睡眠巩固名副其实」这条 P1 出口标准实际未达成。
- **验证**：聊够多轮触发压缩 → 查库应出现 `tier='episodic'` 的行 → 等其满足双门
  （importance/use_count/≥3 天）后 dreaming 自然巩固，无需手工造数据。
- **仍需真机验证**：自动化只能证明候选**落库且形状对**。能否**通过门1**取决于
  `use_count` 累积与 ≥3 天沉淀，只有真机挂着跑才看得到完整巩固闭环。

### P1 出口标准
项目达到设计文档承诺的「完整助手」形态：能主动提问、记住偏好并覆盖矛盾、
待办能在相关对话中被唤起、睡眠巩固名副其实。

> ✅ 四条均已落地。最后一条（睡眠巩固）的上游断点由 P1-6 补齐，链路首次完整；
> 但**端到端效果仍待真机确认**——巩固需时间累积，见 P1-6「仍需真机验证」。

---

## P2 — 生产化收口

让它能长期挂着跑、可对外发布。多为技术债与工程化。

| 项 | 现状 | 来源 |
|---|---|---|
| ~~**读写分离**（读走独立连接池 + spawn_blocking）~~ | ✅ 2026-09-06 `oc-store::reader` 小连接池 + `spawn_blocking`，读连接 `query_only=ON`；内存库回落写线程 | 排查 HIGH-1 |
| **memory.text 索引**（FTS5 或高频列索引） | `LIKE '%term%'` 全表扫描 | 排查 HIGH-1 |
| ~~**写线程 panic 自愈**~~ | ✅ 2026-09-06 `HealthGuard` RAII 翻健康位 + `StoreError::WriterDead` + `is_alive`/`ping` 两级探针；写侧死后读仍可用（降级而非全瘫） | 排查 MEDIUM-1 |
| **idem 幂等缓存 TTL/LRU** | 无淘汰，长期运行内存单调增长 | 排查 MEDIUM-1(server) |
| **会话 actor 空闲淘汰** | actor 永不退出，大量随机 session id 会累积 | 排查 MEDIUM-2(server) |
| ~~**ArcSwap 代际快照 / 配置热更**~~ | 不做热更（P2-5 方案 B），配置变更需重启 `oc serve` | [04 §4.4/§4.8] |
| **provider failover 接线** | `failover_plan`/`resolve_alias` 就绪但无调用方 | [04 §4.6] |
| ~~**双平台 CI matrix**~~ | ✅ 2026-09-06 `ci.yml`：push/PR 跑 ubuntu + windows + macos 三平台 test + clippy；unix socket 在 Linux/macOS 实跑，Windows 走命名管道（P2-7 收口） | [04 §14] M2 硬验收 |
| **oc-core::context 模块** | 上下文加载散在 server::session | [04 §15] M5 |
| **向量语义检索** | sqlite-vec feature 默认关，当前纯词法 | [04 §4.5] |
| **tokenizer 精度** | 字符/4 近似，tiktoken 未接 | [04 §16] |
| **path_guard TOCTOU / 文件写原子性 / 后台输出 channel 无界** | 个人助手信任模型下影响低 | 排查 LOW |

### 语义偏差（择机修正）
- **cron day-of-month / day-of-week**：标准 cron 两者非 `*` 时取 OR，当前实现取 AND
  （个人助手场景多数只用其一，已注明）。

---

## 执行顺序建议

1. **P0-1**（事件丢失）——先复现验证，它极可能就是本周「截断」根因，收益最直接。
2. **P0-2 + P0-3**（审批取消 + 泄漏）——一组，逻辑集中。
3. **P0-4**（阻塞 IO）——独立小改。
4. → P0 出口验证后，进 **P1-1**（ask_user，复用 P0-2/3 的通道）。
5. P1 其余（标注纯函数就绪的接线项，成本小）。
6. P2 按需排期：读写分离 / 写线程自愈优先（可用性瓶颈），其余生产前收口。

**当前位置（2026-09-06）**：**P0 + P1 全部落地**（P1-1~P1-7），**P2 阶段 1 代码部分完成**
（P2-1 读写分离、P2-2 写线程自愈、P2-3 内存淘汰均已落地），剩阶段 1 的真机 7 天观察。
另有 `oc-http`（OpenAI Responses API 兼容层）与打包脚本两项计划外产出。

建议接下来：
- **立即行动（本周）**：
  1. ~~**P1-7 Clippy 警告清零**~~ ✅ 已完成（2026-09-02）
  2. ~~**P1-6 episodic 产出**~~ ✅ 已完成（2026-09-04）
  3. **P1 真机复验**（1 天）—— P1-2~P1-6 只有自动化覆盖，历史上 P0-1/P1-1/P1-5
     的缺陷**全都是真机才暴露**的。P1-6 尤其需要：巩固要等 use_count 累积 + ≥3 天
     沉淀，自动化只能证明候选落库、证不了闭环。
  4. **测试自动化** ✅ 已完成（2026-09-06）—— 原 8 份真机手册（2658 行）改为
     `cargo test` 里的自动化 e2e，并补上 CI 门禁。详见
     [testing/README](../testing/README.md)。
     - 新增 `oc_server::testing` harness（真传输 + 真 store + 脚本化模型），
       顺带消除 18 个测试文件里 36 处 `SessionConfig` 字面量。
     - 新增 `.github/workflows/ci.yml`：push/PR 双平台跑 test + clippy
       —— 即下方 **P2-7 的 CI 门禁部分**，本项已提前落地。
     - 新增 `scripts/e2e/smoke.sh`：进程级冒烟，覆盖 clap/config_loader/paths
       等 `cargo test` 碰不到的一层。
     - 新增 `OC_SOCKET` 环境变量 + `oc serve --socket`：解决 Windows 管道名是
       全局常量、`OC_HOME` 隔离不了连接的问题。
     - **抓到 3 个缺陷**：SSE 双层 `data:` 前缀（🔴 已修，流式对标准客户端不可用）、
       `ERROR_PIPE_BUSY` 未重试（🟠 已修，Windows 并发随机 500）、
       断连收敛盖不住「等模型」窗口（未修，已钉成用例）。
     - 未覆盖：TC-P1-3f（老库 v1 迁移）、TC-H12（真 OpenAI SDK），规格留在
       testing/README 的「未覆盖」一节。
  5. **修掉自动化抓到的两个未修缺陷** —— 其一已完成：
     - ✅ 2026-09-06 断连收敛盖不住「等模型」窗口（🟠）。`run` 在建流 + 等首个
       delta 期间没有事件外发，`emit_inline` 的断连探测根本不会被调用，车道要占到
       空闲看门狗超时（最长 2 分钟）。两处等待叠 `sink.closed()` 修掉。
     - ⬜ `usage.input_tokens` 并发下不可信 —— `Event::Usage` 只有 `session` 没有
       `run_id`，要动 `oc-proto`，跨 crate。
- **短期（2 周内）**：
  6. ~~**P2-1 读写分离**~~ ✅ 2026-09-06（实际 0.5 天）
  7. ~~**P2-2 写线程自愈**~~ ✅ 2026-09-06（实际 0.5 天，与 P2-1 一起做）
  8. ~~**P2-3 幂等缓存 TTL + 会话 actor 空闲淘汰**~~ ✅ 2026-09-06（实际 0.5 天）
     —— P2 阶段 1「可用性基石」代码部分收口
  9. **P2-7 双平台 CI matrix 补齐**（<1 天）← 下一项。CI 骨架已在（第 4 项顺带落地
     了 push/PR 门禁），缺的是 os matrix + MSRV + 分平台传输测试
- **中期（1 个月内）**：P2 阶段 1 真机 7 天测试（内存稳定看 `oc debug` 的 `idem`
  计数与会话行数）→ 阶段 2（P2-4 memory.text 索引）

详细计划见：
- [P1-7-代码质量与技术债.md](P1-7-代码质量与技术债.md)
- [P2-生产化收口详细计划.md](P2-生产化收口详细计划.md)

> 注：P0/P1 每项落地后，同步更新 [04 §15](../design/04-详细设计文档.md) 的落地状态与本文件勾选。

## 变更记录

- 2026-09-06 **P2-3 幂等缓存 TTL + 会话 actor 空闲淘汰完成**，P2 阶段 1 代码部分收口。
  幂等条目加时间戳，`get` 判 TTL + 心跳定期清扫（只靠 get 删会漏掉「写进去就再没人查」
  的冷键，那恰恰最会攒）。会话侧加 `EvictIfIdle` 探针，**判定权在 actor 而非 registry**
  ——registry 只看得到「上次 `get_or_spawn` 时刻」，一条连接握着句柄跑几小时就会被误判
  成空闲。actor 先关接收端再回执，让 registry 的移除判据确定、不靠时序赌。
  **原方案两处不成立**：①「handle drop 自动 abort 任务」——actor 自持 `self_tx`
  用来投 `Finished`，丢条目不会让它退出，任务会永久挂着；② 注册表留一条死句柄会让该
  会话**永久**报「队列已满」（队列其实空着、只是没人收命令），故 `get_or_spawn` 见死
  句柄即换新 + dispatch 重试一次。**比原方案多收两张表**：`diag`/`usage` 同为
  `SessionId` 键、同样无界，只收 actor 等于把增长挪个地方。顺带加
  `DiagnosticsSnapshot.idem_entries` + `oc debug` 显示，否则「跑 48h 观察内存」无从下眼。
  新增 9 项测试（并逐个改坏 6 处实现验证抓得住），全工作区 294 通过、clippy 零告警。
  详见 [P2-生产化收口详细计划](P2-生产化收口详细计划.md)。
- 2026-09-06 **P2-1 读写分离 + P2-2 写线程自愈完成**（一起做，同属存储层）。
  读命令此前是 `WriteCmd`、和写一起排在单写线程队列上，一次全表扫描会拖住所有会话的
  落库；新增 `oc-store::reader` 小连接池 + `Store` 分流，读连接置 `query_only=ON`
  让 SQLite 自己强制「唯一可写连接」而非靠约定。内存库回落写线程（`:memory:` 每条
  连接一个独立库，共享它要 `cache=shared`，会关掉 WAL 并引入 `busy_timeout` 管不着的
  `SQLITE_LOCKED_SHAREDCACHE`）。P2-2 用 `HealthGuard` RAII 翻健康位——panic 展开不会
  走到 `run_loop` 末尾，只有 RAII 盖得住这个最想覆盖的情形；配 `StoreError::WriterDead`
  + `is_alive`（线程还在吗）/`ping`（队列还在推进吗）两级探针。**连带修正**：
  `oc debug` 的写线程探针原本用 `session_list()`，读一挪走那条命令就不再经过写线程，
  写线程死了它也会报 alive，改用 `writer_ping()`。新增 8 项测试，全工作区 285 通过、
  clippy 零告警。详见 [P2-生产化收口详细计划](P2-生产化收口详细计划.md)。
- 2026-09-06 **修「等模型期间断连不收敛」**（测试自动化抓到的缺陷 3）。断连收敛只靠
  `emit_inline` 发送失败探测，而 run 在建流 + 等首个 delta 这段是静默的、探测逻辑
  根本不会被调用，车道要占到空闲看门狗超时（生产默认 120s）。`RunSink::closed()`
  本就是为静默等待期准备的（P0-1 后为 ask_user/审批加的），只是没覆盖等模型这段，
  现在两处等待都叠上。原先记录该缺口的用例
  `disconnect_before_first_delta_falls_back_to_watchdog` 改写为
  `..._converges_fast`，`idle_timeout` 从 2s 调到 120s，使断言到的收敛不可能来自
  看门狗兜底；改前先跑过确认它是红的。
- 2026-09-04 **P1-6（episodic 产出）完成，P1 阶段收口**：补上 dreaming 的上游断点——
  此前全仓无人写 `tier='episodic'`，`dream_candidates` 每轮扫描都是空集，§11.4 的巩固
  能力已完整但**无输入**。落地：core 加 `extract_episode_candidates`（user+assistant 配对
  成一条情节，寒暄按长度滤、不用词表，「记住…」在提取阶段排除以免与 curated 重复）；
  server 加 `flush_episodic`，挂在 compact **调摘要模型之前**（放后面则摘要失败会连沉淀
  一起丢）与 `session.reset` 推进起点之前（之后这段对话不再进任何提示词）。id 用内容
  哈希 → 重复 flush 幂等；`origin=Agent` 而非 Owner（推断 ≠ 用户交代，§4.2）。
  新增 13 测试（含「产出物能过 dreaming 结构门」的闭环断言），全工作区 242 通过、
  clippy 零告警。**留真机**：能否过门1 取决于 use_count 累积 + ≥3 天沉淀，自动化证不了。
- 2026-09-04 **`oc-http`：OpenAI Responses API 兼容层**（计划外产出）。新 crate 在 daemon
  前加一层 HTTP 适配器，`POST /v1/responses` → 内部 NDJSON，支持 SSE 流式/非流式、
  `instructions`、`user`/`previous_response_id` 会话延续、base64 文件输入；不支持的参数
  显式 400 而非静默降级。独立进程（`oc http`），HTTP 侧崩溃不影响会话。
  方案与边界见 [OpenAI-Responses-API-方案](../design/OpenAI-Responses-API-方案.md)，
  Phase 2/3 路线图在该文 §6。真机验证进度见上方「立即行动」第 4 项。
- 2026-09-06 **测试自动化**：把 8 份真机手册（2658 行）改成 `cargo test` 里的自动化
  e2e + 进程级冒烟 + CI 门禁。借鉴 openclaw 的「可执行 vs 人工」分界（`qa suite` /
  `qa manual`）与 live 门控，但不照搬其 190 个 vitest lane / 364 个 scenario YAML
  ——那是 TS 生态启动慢 + 巨型 monorepo 逼出来的，本仓 278 项跑 60s 用不上。
  新增 `oc_server::testing` harness、`scripts/e2e/smoke.sh`、`ci.yml`、
  `OC_SOCKET`/`oc serve --socket`。**抓到 3 个手册没暴露的缺陷**：SSE 双层
  `data:` 前缀（🔴 已修）、`ERROR_PIPE_BUSY` 未重试（🟠 已修）、断连收敛盖不住
  「等模型」窗口（未修，已钉成用例）。详见 [testing/README](../testing/README.md)。
- 2026-09-02 **打包脚本**：Ubuntu（`build-ubuntu.sh` + Docker 交叉构建 + GitHub Actions
  发布 workflow）与 Windows（`build-windows.ps1`）。注意该 workflow 只在打 tag 时构建
  发布物，**不跑测试**——测试门禁在独立的 `ci.yml`（2026-09-06 新增）。
- 2026-09-02 **P1-7 完成**：零 clippy 警告（9 处修复）+ TODO 归档（registry 注释改写指向
  P2 计划）。207 测试全绿。详见 [P1-7-代码质量与技术债.md](P1-7-代码质量与技术债.md)。
- 2026-09-02 **代码评审与计划细化**：全面代码评审（207 测试全绿后）发现 4 条 clippy 警告、
  episodic 产出缺失、读写未分离等问题。新增 [P1-7-代码质量与技术债.md](P1-7-代码质量与技术债.md)
  与 [P2-生产化收口详细计划.md](P2-生产化收口详细计划.md)，将 P2 从原有表格展开为
  每项带改动方案、验收标准、成本估算的详细计划。更新"当前位置"：P1-6 + P1-7 → P2 阶段 1。
- 2026-08-29 建立本计划：合并线程安全排查结论（P0）与设计缺口清单（P1/P2）。
- 2026-08-31 **P0 全部完成**：调研 zeroclaw 传输/流式设计（[06](../research/zeroclaw流式传输调研.md)）后，
  按「per-run 背压流」方案 B 重构 run 事件通道，一次解决 P0-1，并顺带 P0-2/P0-3（同源审批
  通道）；P0-4 独立小改。新增 `RunSink`，删除 `ApprovalHandler`/`EventApprovalHandler`。
  新增回归测试 `long_reply`（>256 delta 不截断）、`approval_cancel`（abort 打断审批 + registry
  不泄漏）。全工作区测试通过，oc-server clippy 零告警。**P0 出口标准达成**，可进 P1-1（ask_user）。
- 2026-08-31 真机测试补记：①修复「同 session 并发提交导致历史顺序错乱→provider 400」
  （用户消息落库推迟到 run 起步时，回归测试 `concurrent_submit`）；②TUI 修复窗口缩放残影 +
  发送后加「排队中…」状态（150ms 去抖，避免秒起步时闪烁）。③新增 P1-5 缺陷条目（定时提醒
  未走 cron，见上）。
- 2026-08-31 **P1-1（ask_user）完成**：新增阻塞式「向用户提问」工具，回执走与审批同构的
  自由文本通道（复用 P0-2 取消 + P0-3 RAII 清理，一次重构覆盖两条交互路径）。proto 加
  `UserInput`/`UserReply`，tools 加 `InputGate`+`ask_user`，server 加 `InputRegistry`+
  `request_input`，TUI 加 `pending_input` 输入态。新增回归 `ask_user`（回答回喂 + abort 打断 +
  registry 不泄漏）；全工作区测试通过，oc-server 库 clippy 零告警。下一项：P1-2（standing intent
  接线）或 P1-5（cron 引导，真机暴露的助手能力缺失）。
- 2026-09-01 **新增 P1-6（episodic 产出，§11.5 flush）**：写 P1-4 测试用例时核对代码发现
  **dreaming 在真机上永远空转**——全仓没有任何代码写 `tier='episodic'`，而
  `dream_candidates` 只取 episodic；唯一的记忆写入路径 `persist_explicit_memory` 直接写
  curated，绕过了 episodic→巩固这条链。即 P1-4 的巩固能力已完整但**无输入**，只能靠手工
  塞数据验证。故补 P1-6 单列此断点，并在 [04 §11.5](../design/04-详细设计文档.md) 标 ❌ 未落地、
  P1 出口标准里注明「睡眠巩固」尚未真正达成。同时给「执行顺序建议」补当前位置与下一步。
- 2026-09-01 **P1-4（dreaming 重写 MEMORY.md）完成**：MEMORY.md 此前与 USER.md 同为
  「onboard 建了但无人读写」的死文件。落地巩固模型轮 + 完整乐观并发（读 hash → 模型重写
  → 落盘前重校验 hash → 一致才原子 rename，否则退化 append 不覆盖）。纯策略（prompt 组装、
  `decide_write`）在 core，文件 IO/模型轮在 server，路径由 CLI 经 `SessionConfig::soul_dir`
  传入（`None` 时不碰文件，既有 `scan()` 签名保留兼容）。写安全是重点：模型空输出/超时保持
  原文件不清空、同目录 `.tmp`+rename（跨盘 rename 非原子）、Windows 先删目标再 rename、
  失败清 tmp、系统提示词禁编造。新增 12 测试（core 5 + server 7，含用本地 `RacingProvider`
  在模型生成期间改文件来真实制造并发冲突），全工作区 188 用例通过，无新增 clippy 告警、
  无新增依赖。未做：巩固轮每日 token 预算上限、USER.md 落盘。
- 2026-09-01 **文档目录分类整理**：`docs/` 12 份文档曾平铺一层，出现两个撞号的 `07-`、
  三份无编号文档、README 里 3 条指向已改名文档的坏链。重组为 `design/`（01~04 保留编号，
  有阅读顺序）、`plan/`、`testing/`、`research/` 四类，其余文档去掉全局编号改用语义命名
  （避免再撞号）。全部 `git mv` 保留文件历史；修好 50+ 处内部链接（跨目录引用与
  `../crates/` → `../../crates/`）并加了链接校验；新增 [docs/README.md](../README.md) 总索引
  + 命名约定。顺手更正 README 里过时的测试数（110 → 176）。
- 2026-09-01 **P1-3（偏好 supersede）完成**：探查先纠正两处认知——①USER.md 是 onboard 建了
  但**全仓无人读写**的死文件（只有 SOUL.md 被读），偏好实际都进 `memory` 表走 Lane1 注入，
  故落点定为 **DB 内 supersede**，文件侧留 P1-4；②现状不是"纯 append"而是按内容哈希**逐字
  去重**，真缺口是同主题不同表述（"我用 VS Code" vs "我改用 Neovim"）。落地：新增
  `extract_pref_key` 纯词法抽主题（6 类词表，不调模型，保守未命中即退回原路径——Replace 会
  删旧条目，误判会丢信息）、`memory` 加 `pref_key` 列（迁移 v2）、`memory_by_pref_key`/
  `delete_memory` ops、`persist_explicit_memory` 接 supersede（先写新后删旧 + 审计）。
  修 bug：`search_candidates` 的 mapper 加了 `pref_key` 取值但漏改其**动态拼接**的 SELECT
  列表，被既有 Lane1 测试抓到（另两处静态 SQL 已同步）。新增 9 测试（含 v1→v2 迁移保数据），
  全工作区 176 用例通过，clippy 无新增告警。
- 2026-08-31 **P1-2（standing intent 触发链）完成**：先调研 zeroclaw（结论：它**没有**话题
  触发式提醒，功能拆成向量记忆/cron/SOP 三块无一命中；见 [07](P1-2-standing-intent方案.md)），
  确认朴素方案（词法预筛 standing_intent 表）合理。落地：store 补 `intent_*` ops、session
  `intent_scan` 触发链（复用 bootstrap 注入 + per-row anti-nagging）、`intent.add/list/rm`
  proto + `oc intent` CLI、配置接线（接上此前死键）。修两个 bug：expiry 绝对/相对换算
  （哨兵 0=永不过期 与 `max(0)` 钳位冲突，把已过期翻转成永不过期）、keywords 空格分隔
  导致含空格关键词被劈开（触发面变宽）——后者是逐行复查 diff 时抓到的，测试"往返不丢字"
  但值已变形。新增回归 10 项，全工作区 166 用例通过。创建入口用显式 CLI（不做自动解析，
  zeroclaw 验证过脆弱）；模型工具化留 P1-5。
- 2026-08-31 **修复：静默等待期 client 断连不收敛**（真机 TC-P1-1e 暴露）。ask_user/审批
  发出请求后卡在 `rx.await`，期间不 send，故 P0-1 的「send 失败探测断连」机制**对静默期失效**；
  client 掉线后 run 干等到空闲看门狗兜底才释放车道（实测约 34s，期间新消息排队）。修法：
  ① 新增 `RunSink::closed()`，`request_input`/`request_approval` 的 `select!` 叠加它 → 断连即
  cancel 收敛；② conn 收尾由 `writer_handle.await` 改 `abort()`——活跃 run 持有的 `out_tx`
  clone 会让写任务永不退出、`closed()` 永不触发。此缺陷**审批路径同源同病**，一并修复。
  新增回归 `client_disconnect_during_ask_user_converges_fast`（断言 <2s 收敛，非看门狗兜底）。
- 2026-09-01 **P1-5 首版（cron 工具暴露 + 提示引导）**：解决 2026-08-31 真机暴露的
  「模型不知道 cron 能力 → 退化到 run 内轮询等待（`sys:now` + `Start-Sleep`/`timeout`）
  → 撞 loop detection → 提醒设置失败」。落地：新增 `CronTool` 暴露 `cron_add` 让模型直接调用，
  经 `ToolCtx::cron` 门 → server `cron_rx` 消费循环 → `proactive` 写库 + 排期；
  系统提示词加「定时/延时提醒用工具，不要用 shell 睡眠阻塞等待」。
  > **勘误**：本条原先写「新增回归 `cron_tool_e2e`」——该测试并不存在，首版只加了
  > 真机用例文档，没有自动化覆盖。这也是当晚真机三个缺陷全都漏网的直接原因。
- 2026-09-01 **P1-5 补修（时区 / list+rm / 亚分钟延时）**：首版真机复测发现模型**确实
  改用工具了**，但提醒**一次都没响**——三个独立缺陷叠加（详见上方 P1-5「真机复测」表）：
  ① `next_fire` 一律按 UTC 解释表达式，`tz` 列自 M6 起存了没人读，东八区偏 8 小时；
  ② 只暴露了 `add`，模型无法自查，用户说「没收到」时它只能猜、猜错并改用更差的替代方案；
  ③ cron 最小粒度是分钟，「90 秒后」只能凑成每日重复任务。
  修法：`next_fire(expr, after, tz)` 按 IANA 时区解释（新依赖 `time-tz`；时区名由 CLI
  探测传入，因 `time` 取本地偏移在多线程下不可靠；拼错的 tz 报错而非静默按 UTC）；
  `cron` 工具统一四个 op（add/delay/list/rm），回执带本地墙上时间与剩余时长；
  `delay` 走 `@once` 标记 + 绝对 `next_at`，触发后即删，≤1h 另挂精确 timer。
  一次性任务的两条触发路径（timer + 心跳兜底）用 `DELETE` 的 rows-affected 做原子认领；
  重复任务**刻意保持先跑后重排**——先占位再跑的话，进程中途被杀会把 `next_at` 永久留空，
  一条每日提醒从此再不触发（比重复提醒一次糟得多）。
  顺带修 `oc cron list` 只印 unix 秒（看不出差了 8 小时，是这缺陷难发现的原因之一）、
  `oc cron add --tz` 默认值 UTC → 本机时区。
  新增 19 项自动化测试（core cron +7 含东八区回归与 DST 跳表/对照组，该模块共 14；
  `tests/cron_tool.rs` 9 项四 op；`tests/proactive.rs` +3 按行 tz 重排/用后即删/并发只触发一次），
  全工作区 207 用例通过，clippy 无新增告警。
  **当前位置**：P0 + P1-1~P1-5 已落地，剩 **P1-6**（episodic 产出，dreaming 的上游断点）。
  回归见 [cron_tool.rs](../../crates/oc-server/tests/cron_tool.rs) + [e2e_cron_push.rs](../../crates/oc-server/tests/e2e_cron_push.rs)
  并跑一遍 P1-2/P1-3/P1-4，再做 P1-6。

