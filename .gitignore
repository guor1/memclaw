# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# 导出的协议 schema（可由 `oc doctor --dump-schema` 复现）
/schema

# 本地运行时数据
/.oc
*.sqlite
*.sqlite-wal
*.sqlite-shm

# 早期用 OC_HOME 隔离测试留下的目录（现已改用默认 ~/.oc，见 docs/testing/）
/.octest

# 真机测试临时构造的请求体
/.e2e_req*.json

# Claude Code 本地个人设置（不提交）
.claude/settings.local.json

# 编辑器 / OS
.DS_Store
*.swp
settings.yaml
dist-windows/